namespace MyTube.Model
{
    public enum State
    {
        UNKNOWN,
        TRIM,
        KNOWN,
        SEARCH,
        DEFAULT,
        CHARACTER,
        PARTITION,
        PLAYLIST,
        ADOPTION,
    }
}